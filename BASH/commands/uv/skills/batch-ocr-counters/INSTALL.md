# skills/batch-ocr-counters/INSTALL.md

## Python dependencies

```
pip install -r requirements.txt
```

## System dependencies

```
sudo apt install tesseract-ocr
```

## ocr-pic skill
- Ensure `ocr-pic.py` (from the ocr-pic skill) is present in the same directory or in your PATH.
