Title: file_utils.py — plan
Date: 2026-05-16

Referenced files:
- (external) pandas
- (stdlib) csv, logging

Summary:
file_utils.py is a small I/O helper providing read_file, read_csv_builtin, read_csv_pandas and read_excel. It contains GUI-side popups for file errors and uses pandas when available.

Recommendations (retain full functionality):
1. Add precise type annotations and return types (Optional[str], List[List[str]], DataFrame|None).
2. Replace show_error_popup side-effect with injectable callback; keep non-GUI pure functions for library use.
3. Prefer pathlib.Path for path handling and explicit encoding parameters.
4. Improve exception granularity and remove broad excepts; log and re-raise where appropriate.
5. Add unit tests for edge cases (missing file, bad encoding, empty CSV) and CI entry.
6. Document API and expected return types; move pandas fallback behind a clear runtime check.
7. Ensure PEP8 (line length, imports order) and add module docstring.

Todos:
- [ ] Add function signatures with typing and docstrings.
- [ ] Extract GUI popup into a ui helper and inject it.
- [ ] Add tests for read_csv_builtin/read_csv_pandas/read_excel.
- [ ] Run flake8/black and fix issues.
