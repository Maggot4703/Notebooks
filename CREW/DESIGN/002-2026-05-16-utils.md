Title: utils.py — plan
Date: 2026-05-16

Referenced files:
- file_utils.py (local)
- PIL (external)

Summary:
utils.py provides small helpers (log_progress_md, show_user_error, spacer) and higher-level CSV/Excel processing wrappers used across the app.

Recommendations:
1. Add full type hints and return annotations; avoid returning mixed types.
2. Move heavy imports (PIL) into function scope where only used to avoid import-time cost.
3. Reduce coupling to file_utils by depending on interfaces rather than module-level imports where feasible.
4. Make logging consistent (module logger) and avoid printing to stdout in library code.
5. Add unit tests for helper functions and integration tests that exercise CSV/Excel flows.
6. Add module docstring, consistent naming and PEP8 import ordering.

Todos:
- [ ] Add typing and docstrings.
- [ ] Move PIL import inside functions that need it.
- [ ] Add tests and CI entry.
