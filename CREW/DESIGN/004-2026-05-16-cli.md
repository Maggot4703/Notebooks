Title: cli.py — plan
Date: 2026-05-16

Referenced files:
- image_utils.py (local)
- utils.py (local)
- deepseek_integration.py (local, optional)

Summary:
cli.py defines the CLI parser, command registry and handlers that call into other modules. It holds command implementations and error handling for CLI mode.

Recommendations:
1. Centralize command handlers into a dedicated handlers/ directory or module to keep parser simple.
2. Ensure consistent use of types for arguments and validate inputs early.
3. Fix API mismatch: grid-image currently constructs a PIL Image and calls overlay_grid(img, ...) while image_utils.overlay_grid accepts a file path. Either update overlay_grid to accept Image or change cli to pass path.
4. Use structured logging and avoid printing from library functions; return explicit exit codes.
5. Add unit tests for each handler using temporary files (pytest tmp_path).
6. Keep COMMAND_REGISTRY but consider using click or argparse subparsers with clear handler signatures.
7. Ensure PEP8 and remove any unnecessary try/except that silences errors.

Todos:
- [ ] Fix overlay_grid API mismatch.
- [ ] Add tests for crop-csv, read-csv, read-excel.
- [ ] Refactor handlers into smaller modules.
